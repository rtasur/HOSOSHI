import{useEffect,useState}from'react';import{api}from'../services/api';export default function AuditPage(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{api.get('/audit').then(r=>setRows(r.data)).catch(()=>{})},[]);return <div><div className="page-head"><div><div className="eyebrow">AUDIT TRAIL</div><h1>Audit activity</h1><p>Recent access and operational events.</p></div></div><section className="panel table-panel"><div className="table-scroll"><table><thead><tr><th>TIME</th><th>USER</th><th>ACTION</th><th>RESOURCE</th><th>DETAILS</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.createdAt).toLocaleString()}</td><td>{r.username}</td><td className="mono">{r.action}</td><td>{r.resourceType}</td><td>{r.details}</td></tr>)}</tbody></table></div></section></div>}
