import {FormEvent,useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../auth/AuthContext';import{ArrowRight,ShieldCheck}from'lucide-react';
export default function LoginPage({signup=false}:{signup?:boolean}){const{login,signup:register}=useAuth();const nav=useNavigate();const[form,setForm]=useState({username:'',password:'',fullName:'',email:'',requestedRole:'INVESTIGATOR'});const[err,setErr]=useState('');const[msg,setMsg]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setErr('');try{if(signup){const r=await register(form);setMsg(r.message)}else{await login(form.username,form.password);nav('/')}}catch(x:any){const message=x.response?.data?.message||x.response?.data?.error||(x.code==='ERR_NETWORK'?'HOSOSHI backend is not reachable. Run START-HOSOSHI.cmd and check VERIFY-HOSOSHI.cmd.':x.response?.status===401?'Invalid username or password.':'Unable to complete request');setErr(message)}};return <div className="login-shell"><div className="login-brand"><img src="/assets/hososhi-mark.png"/><div><b>HOSOSHI</b><span>NETWORK INTELLIGENCE PLATFORM</span></div></div><div className="login-card"><div className="eyebrow">{signup?'ACCESS REGISTRATION':'AUTHORIZED ACCESS'}</div><h1>{signup?'Request an account':'Secure sign-in'}</h1><p className="muted">{signup?'Submit your details for administrator approval.':'Use an approved Hososhi account to access authorized investigations.'}</p>{msg&&<div className="notice success">{msg}</div>}<form onSubmit={submit}>{signup&&<><label>Full name<input required value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Requested role<select value={form.requestedRole} onChange={e=>setForm({...form,requestedRole:e.target.value})}><option value="INVESTIGATOR">Investigator</option><option value="INVESTIGATION_SUPERVISOR">Investigation Supervisor</option><option value="INTELLIGENCE_ANALYST">Intelligence Analyst</option><option value="DATA_OPERATOR">Data Operator</option><option value="AUDITOR">Auditor</option></select></label></>}<label>Username<input required value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label>Password<input required minLength={6} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{err&&<div className="notice error">{err}</div>}<button className="primary wide" type="submit">{signup?'Submit registration':'Enter Hososhi'} <ArrowRight size={15}/></button></form><div className="login-footer"><Link to={signup?'/login':'/signup'}>{signup?'Back to sign in':'Request an account'}</Link><span className="demo-note"><ShieldCheck size={13}/> Administrator approval required</span></div></div></div>}
